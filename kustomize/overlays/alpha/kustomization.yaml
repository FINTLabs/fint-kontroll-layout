apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - patch: |-
      - op: replace
        path: "/spec/basePath"
        value: /alpha/fintlabs-no
    target:
      kind: FLAISSso
      name: fint-kontroll-sso